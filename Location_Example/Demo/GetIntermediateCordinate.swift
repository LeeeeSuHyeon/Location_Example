//
//  GetIntermediateCordinate.swift
//  Location_Example
//
//  Created by 이수현 on 2024/03/14.
//

import Foundation

class GetIntermediateCordinate {
    
//    static func getCordinates(source: LocationDetails, destination: LocationDetails, mode: String, success: @escaping([Step]) -> Bool, failure: @escaping(String) -> Void) {
//        var steps = [Step] ()
//        
////        steps = Step().intermediateRouteInfo(leg)
//        // Step()의 intermediateRouteInfo() 메서드를 호출하여 [Step] 반환
//
//    }
    
    
    static func getCordinates(source: LocationDetails, destination: LocationDetails) -> [Step] {
        var steps = [Step] ()
        
//        steps = Step().intermediateRouteInfo(leg)
        // Step()의 intermediateRouteInfo() 메서드를 호출하여 [Step] 반환
        return steps
    }
}
